import Ember from 'ember';

export default Ember.Route.extend({
	init: function(){
		console.log("ladder router");
    }
    //setupController: function(controller, model){
    //    this._super(controller, model);
    //    controller._generateStatistics();
    //}
});
